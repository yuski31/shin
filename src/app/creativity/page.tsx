import CreativityInterface from '@/components/creativity/CreativityInterface';

export default function CreativityPage() {
  return (
    <div className="min-h-screen bg-gradient-to-br from-purple-50 to-blue-50">
      <CreativityInterface />
    </div>
  );
}